<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                {{ 'title.welcome'|trans }}
            {% endblock %}
        </title>
        {% block stylesheets %}
            <link
                    href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
                    rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
                    crossorigin="anonymous">
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css">
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}
    </head>

    <header>
        <nav class="navbar navbar-expand-md navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('ebay_index') }}">Ebay.com</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarsExample04">
                    <ul class="navbar-nav me-auto mb-2 mb-md-0">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('ebay_users') }}" tabindex="-1" aria-disabled="true">{{ 'title.users_list'|trans }}</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="dropdown04" data-bs-toggle="dropdown" aria-expanded="false">{{ 'title.objects_list'|trans }}</a>
                            <ul class="dropdown-menu" aria-labelledby="dropdown04">
                                <li><a class="dropdown-item" href="{{ path('ebay_records') }}">{{ 'title.records_list'|trans }}</a></li>
                                <li><a class="dropdown-item" href="{{ path('ebay_genres') }}">{{ 'title.genres_list'|trans }}</a></li>
                                <li><a class="dropdown-item" href="{{ path('ebay_authors') }}">{{ 'title.authors_list'|trans }}</a></li>
                                <li><a class="dropdown-item" href="{{ path('ebay_reservations') }}">{{ 'title.reservations_list'|trans }}</a></li>
                            </ul>
                        </li>
                        {% endif %}
                    </ul>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <div class="m-2">{{ app.user.email }}</div>
                        <div>
                            <a class="btn btn-outline-light" href="{{ url('app_logout') }}">{{ 'title.logout'|trans }}</a>
                        </div>
                    {% else %}
                        <div>
                            <a class="btn btn-outline-light" href="{{ url('app_login') }}">{{ 'title.login'|trans }}</a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </nav>
    </header>

    <body>
        {% include 'default/_flash_messages.html.twig' %}

        {% block body %}{% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
            <script
                    src="https://code.jquery.com/jquery-3.6.0.slim.min.js"
                    integrity="sha256-u7e5khyithlIdTpu22PHhENmPcRdFiHRjhAuHcs05RI="
                    crossorigin="anonymous"></script>
            <script
                    src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"
                    integrity="sha384-7+zCNj/IqJ95wo16oMtfsKbZ9ccEh31eOz1HGyDuCQ6wgnyJNSYdrPa03rtR1zdB"
                    crossorigin="anonymous"></script>
            <script
                    src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"
                    integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13"
                    crossorigin="anonymous"></script>
        {% endblock %}
    </body>
</html>
